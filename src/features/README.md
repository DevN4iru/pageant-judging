# Frontend Features

Future React code should be split by feature.

Do not keep adding major screens to `src/App.jsx`.

Target:
- `src/App.jsx` becomes a thin shell/router.
- feature folders own their pages, components, hooks, and services.
